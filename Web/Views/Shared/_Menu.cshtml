<nav class="navbar navbar-expand-lg navbar-dark bg-primary opacity-100">
    <div class="container-fluid">
        @*@Html.ActionLink("Condominios", "Index", "Informacion", null, new { @class = "navbar-brand" })*@

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" id="logo" href="#">
                        Home

                    </a>
                </li>

                @if (Web.Security.AutorizeView.IsUserInRole(new string[] { "Administrador" }))
                {
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Mantenimientos</a>
                        <div class="dropdown-menu">
                            @if (Web.Security.AutorizeView.IsUserInRole(new string[] { "Administrador" }))
                            {
                                @*Enlace a la lista de Residencias*@
                                @Html.ActionLink("Residencias", "Index", "Residencia", null, new { @class = "dropdown-item" })
                            }
                            <div class="dropdown-divider"></div>
                            @if (Web.Security.AutorizeView.IsUserInRole(new string[] { "Administrador" }))
                            {
                                @*Enlace a la lista de Usuarios*@
                                @Html.ActionLink("Usuarios", "Index", "Usuario", null, new { @class = "dropdown-item" })
                            }
                            <div class="dropdown-divider"></div>
                            @if (Web.Security.AutorizeView.IsUserInRole(new string[] { "Administrador" }))
                            {
                                @*Enlace a la lista de Reservaciones*@
                                @Html.ActionLink("Reservaciones", "IndexAdmin", "ReservaAreaComun", null, new { @class = "dropdown-item" })
                            }
                            <div class="dropdown-divider"></div>
                            @if (Web.Security.AutorizeView.IsUserInRole(new string[] { "Administrador" }))
                            {
                                @*Enlace a la lista de Informacion*@
                                @Html.ActionLink("Informacion", "IndexAdmin", "Informacion", null, new { @class = "dropdown-item" })
                            }
                            <div class="dropdown-divider"></div>
                            @if (Web.Security.AutorizeView.IsUserInRole(new string[] { "Administrador" }))
                            {
                                @*Enlace a la lista de Incidencias*@
                                @Html.ActionLink("Incidencias", "IndexAdmin", "Incidencia", null, new { @class = "dropdown-item" })
                            }
                        </div>
                    </li>
                }

                @if (Web.Security.AutorizeView.IsUserInRole(new string[] { "Administrador" }))
                {
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Gestión financiera</a>
                        <div class="dropdown-menu">
                            @if (Web.Security.AutorizeView.IsUserInRole(new string[] { "Administrador" }))
                            {
                                @Html.ActionLink("Rubros", "Index", "RubroCobro", null, new { @class = "dropdown-item" })
                            }
                            <div class="dropdown-divider"></div>
                            @if (Web.Security.AutorizeView.IsUserInRole(new string[] { "Administrador" }))
                            {
                                @Html.ActionLink("Planes de cobro", "Index", "PlanCobro", null, new { @class = "dropdown-item" })
                            }
                            <div class="dropdown-divider"></div>
                            @if (Web.Security.AutorizeView.IsUserInRole(new string[] { "Administrador" }))
                            {
                                @*Enlace a la lista de Asignaciones de Planes*@
                                @Html.ActionLink("Asignaciones de Planes", "Index", "AsignaPlanCobro", null, new { @class = "dropdown-item" })

                            }
                            <div class="dropdown-divider"></div>
                            @if (Web.Security.AutorizeView.IsUserInRole(new string[] { "Administrador" }))
                            {
                                @*Enlace a la Gestión de Deudas*@
                                @Html.ActionLink("Gestión de Deudas", "GestionDeudas", "AsignaPlanCobro", null, new { @class = "dropdown-item" })

                            }
                            <div class="dropdown-divider"></div>
                            @if (Web.Security.AutorizeView.IsUserInRole(new string[] { "Administrador" }))
                            {
                                @*Enlace a la lista de Historial de pagos*@
                                @Html.ActionLink("Historial de Pagos", "HistorialPagos", "AsignaPlanCobro", null, new { @class = "dropdown-item" })
                            }
                        </div>
                    </li>
                }

                @if (Web.Security.AutorizeView.IsUserInRole(new string[] { "Residente" }))
                {
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Menú Principal</a>
                        <div class="dropdown-menu">
                            @if (Web.Security.AutorizeView.IsUserInRole(new string[] { "Residente" }))
                            {
                                @Html.ActionLink("Incidencias", "IndexResidente", "Incidencia", null, new { @class = "dropdown-item" })
                            }
                            <div class="dropdown-divider"></div>
                            @if (Web.Security.AutorizeView.IsUserInRole(new string[] { "Residente" }))
                            {
                                @Html.ActionLink("Reservaciones", "IndexResidente", "ReservaAreaComun", null, new { @class = "dropdown-item" })
                            }
                            <div class="dropdown-divider"></div>
                            @if (Web.Security.AutorizeView.IsUserInRole(new string[] { "Residente" }))
                            {
                                @Html.ActionLink("Estado de Cuenta", "EstadoCuenta", "AsignaPlanCobro", null, new { @class = "dropdown-item" })
                            }
                        </div>
                    </li>
                }
                @* Texto Usuario, Accion, Controlador*@
                <li>
                    @Html.ActionLink("Acerca de", "About", "Home", null, new { @class = "nav-link" })
                </li>
                <li>@Html.ActionLink("Contacto", "Contact", "Home", null, new { @class = "nav-link" })</li>

                <li class="nav-item dropdown">
                    @* Información usuario *@
                    @{
                        String nombre = "";
                        String correo = "Perfil";

                        if (Session["User"] != null)
                        {
                            Infraestructure.Models.Usuario usuario = new Infraestructure.Models.Usuario();
                            usuario = (Infraestructure.Models.Usuario)Session["User"];
                            nombre = $"{usuario.Nombre} {usuario.Apellidos}";
                            correo = usuario.Correo;
                        }
                    }
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown"
                       href="#" role="button" aria-haspopup="true"
                       aria-expanded="false"><i class="fa-solid fa-user"></i> @correo</a>

                    <div class="dropdown-menu">
                        @if (Session["User"] != null)
                        {
                            <div class="dropdown-item">
                                @* Nombre Usuario *@
                                Usuario: @nombre
                            </div>
                        }

                        @if (Session["User"] == null)
                        {
                            <a class="dropdown-item"
                               @*Accion, Controlador*@
                               href="@Url.Action("Index","Login")">
                                <i class="fa-solid fa-user-plus"></i>
                                Login
                            </a>
                        }
                        else
                        {
                            <a class="dropdown-item"
                               href="@Url.Action("Logout","Login")">
                                <i class="fa-solid fa-right-from-bracket"></i>
                                Logout
                            </a>
                        }
                    </div>
                </li>
            </ul>
            <form class="d-flex">
                <input class="form-control me-sm-2" type="search" placeholder="Search">
                <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
            </form>
        </div>
    </div>
</nav>
